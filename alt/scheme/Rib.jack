/*
Type for a word that points to a rib in memory.

No operations at the moment, because the overhead of calling them is prohibitive in the absence
of inlining.

    var Rib foo;

    let foo = ...;

    let x = foo[0];
    let y = foo[1];
    let z = foo[2];

*/
class Rib {
    field int x, y, z;

    // TODO: get the compiler to inline this, so we can actually afford to use it
    // method int x() {
    //     return x;
    // }

    // TODO: y(), z(), setX/Y/Z()?
}
